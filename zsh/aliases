# directories
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ......="cd ../../../../.."

# paths
alias dot="cd ${DOTFILES}"
alias src="cd ${HOME}/src"
alias zz="cd ${ZDOTDIR}"

# ls
alias ls="ls --color=auto"
alias ll="ls -l"
alias la="ls -l --all"
alias l="ls"

# source
alias load="source ${ZDOTDIR}/.zshrc"

# wsl
alias wexp="explorer.exe"
alias wsrc="cd $(wslpath "$(wslvar USERPROFILE)")"
alias cmd="cmd.exe"
