# Let gpg-agent act as the SSH agent. Fish exports SSH_AUTH_SOCK from
# `gpgconf --list-dirs agent-ssh-socket`.
enable-ssh-support

# Cache passphrases/PINs for 10 minutes by default.
default-cache-ttl 600

# Never keep cached passphrases/PINs for more than 2 hours.
max-cache-ttl 7200
