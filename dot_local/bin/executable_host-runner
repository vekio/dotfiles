#!/usr/bin/env bash
set -euo pipefail

executable="$(basename "$0")"
target="$executable"

case "$executable" in
  host-*) target="${executable#host-}" ;;
esac

exec flatpak-spawn --host \
  env --unset=FLATPAK_ID \
  "$target" "$@"
