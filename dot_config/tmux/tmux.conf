# Keep the default Ctrl-b prefix and add only a small set of common helpers.
set -g mouse on
set -g history-limit 50000
set -g renumber-windows on

# Open new windows and panes in the current pane's directory.
bind c new-window -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Vim-style pane navigation; the default arrow bindings remain available.
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"
